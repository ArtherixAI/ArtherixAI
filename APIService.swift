import Foundation

class APIService {

    func sendToAI(message: String) async -> String {
        // пока заглушка, но структура уже под реальный API
        // сюда потом вставим OpenAI или твой backend

        return "Response placeholder for: \(message)"
    }
}
